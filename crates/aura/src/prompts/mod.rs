// Prompt templates are stored as .md files in this directory and loaded
// via `include_str!()` from the modules that use them. This mod.rs exists
// only to satisfy the `pub mod prompts;` declaration in lib.rs.
