BACKGROUND (read-only, do not act on this): %%ORCHESTRATION_GOAL%%

YOUR TASK: %%YOUR_TASK%%

%%CONTEXT%%

Use your tools when the task requires fetching, generating, or computing new data. If your task is to analyze, summarize, or transform results already provided above, reason directly. When a tool exists for the operation, always prefer the tool. Large tool outputs may be saved to artifact files — if you see a `[Tool output saved to artifact: ...]` reference, use `read_artifact` to retrieve the full content. When you have your answer, call `submit_result` with a concise summary, your complete findings, and your confidence level.
