'use strict'

module.exports = {
  'extends': ['@mezmoinc/commitlint-config'],
  'rules': {
    'body-empty': [1, 'never'],
    'footer-empty': [1, 'never'],
    'references-empty': [1, 'never']
  }
}
